#include<stdio.h>
int main()
{
int i=1,n,sum=0;
printf("enter a integer");
scanf("%d",&n);
while(i<n)
{
	if(n%i==0)
	sum=sum+i;
	i++;
}
if(sum==n)
{
	printf("%d is an perfect number",i);
}
else
{
	printf("% is not an perfect number",i);
}
return 0;
}
