#include<stdio.h>
#define N 1000
int main(){
	float arr[N];
	int n;
	printf("enter the size of array:");
	scanf("%d",&n);
	for(int i=0;i<n;i++)
	{scanf("%f",&arr[i]);
	}
	printf("reversed array: ");
	for(int i= n-1;i>=0;i--)
	{printf("%f",(arr[i]));
	printf("  ");
	}
	return 0;
	
}

